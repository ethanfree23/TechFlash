/** Placeholder metrics — merge after buildAdminCommandCenterModel. See mergeMockSupplements. */
export function getMockAdminSupplements() {
  return {
    subscriptionRevenueCents: 0,
    disputesOpenCount: 0,
    openSupportIssuesCount: 0,
    licensesExpiringSoonCount: 0,
  };
}
